import { PenLine, Sparkles } from "lucide-react";
import { StepProgress } from "../components/StepProgress";
export function AdditionalMessageScreen({ value, onChange, onGenerate, error, loading }: { value: string; onChange(value: string): void; onGenerate(): void; error?: string; loading?: boolean }) { return <section className="content-screen"><StepProgress current={3} /><div className="screen-heading"><p className="eyebrow">再补充一点</p><h1>有什么想特别说的？</h1><p>一句话也可以，AI 会保留你的真实语气。</p></div><label className="message-box"><PenLine size={20} /><textarea value={value} maxLength={120} onChange={(e) => onChange(e.target.value)} placeholder="例如：酸汤很开胃，服务员还主动帮忙添了汤……" /><span>{value.length}/120</span></label>{error && <p className="inline-error">{error}</p>}<div className="inspiration"><Sparkles size={18} /><span><b>小提示</b>味道、服务、环境或某个难忘细节都可以写。</span></div><div className="sticky-action"><button className="primary-button" disabled={loading} onClick={onGenerate}><Sparkles size={19} />{loading ? "正在提交…" : "生成我的评价"}</button></div></section>; }
