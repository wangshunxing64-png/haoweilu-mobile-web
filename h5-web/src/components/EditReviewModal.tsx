import { useEffect, useRef, useState } from "react";
import { X } from "lucide-react";
export function EditReviewModal({ content, onClose, onSave }: { content: string; onClose(): void; onSave(value: string): void }) { const [value, setValue] = useState(content); const ref = useRef<HTMLTextAreaElement>(null); useEffect(() => { ref.current?.focus(); const key = (e: KeyboardEvent) => e.key === "Escape" && onClose(); window.addEventListener("keydown", key); return () => window.removeEventListener("keydown", key); }, [onClose]); return <div className="modal-backdrop" onMouseDown={(e) => e.target === e.currentTarget && onClose()}><section className="modal-card" role="dialog" aria-modal="true" aria-labelledby="edit-title"><button className="modal-close" aria-label="关闭" onClick={onClose}><X /></button><p className="eyebrow">保留你的语气</p><h2 id="edit-title">编辑评价</h2><textarea ref={ref} value={value} maxLength={1000} aria-label="评价内容" onChange={(e) => setValue(e.target.value)} /><span className="counter">{value.length}/1000</span><button className="primary-button" disabled={!value.trim()} onClick={() => onSave(value.trim())}>保存修改</button></section></div>; }
