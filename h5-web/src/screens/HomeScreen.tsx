import { ArrowRight, MapPin, Sparkles } from "lucide-react";
import type { PublicConfig } from "../types/api";
export function HomeScreen({ config, onStart }: { config: PublicConfig; onStart(): void }) { return <section className="home-screen"><div className="hero-orbit"><div className="hero-mark"><span>味</span><Sparkles size={18} /></div></div><p className="eyebrow">AI 餐饮评价助手</p><h1>把这一餐的<br /><em>真实感受</em>记下来</h1><p className="hero-subtitle">选一选、说一说，由 AI 帮你整理成自然、有温度的用餐评价。</p><div className="store-card"><span className="store-pin"><MapPin size={20} /></span><span><small>{config.merchant.name}</small><b>{config.store.name}</b></span><i>当前门店</i></div><button className="primary-button" onClick={onStart}>开始记录美味<ArrowRight size={20} /></button><p className="privacy-note">无需登录 · 内容由你确认 · 仅用于本次体验</p></section>; }
