export function StepProgress({ current, total = 6 }: { current: number; total?: number }) { return <div className="progress-wrap" aria-label={`第 ${current} 步，共 ${total} 步`}><div className="progress-copy"><span>记录真实体验</span><span>{String(current).padStart(2, "0")} / {String(total).padStart(2, "0")}</span></div><div className="progress-track"><i style={{ width: `${current / total * 100}%` }} /></div></div>; }
